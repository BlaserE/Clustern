# Inventory
The `inventory.ini` or `hosts.yml` contains all the defined server's information. 
This is the map of the infrastrucutre.

It tells Ansible the IP address and connection details for any nodes, such as SSK keys or passwords. 

I have provided an exemple, simply copy it to `inventory.yml` and rename accordingly to your configuration.